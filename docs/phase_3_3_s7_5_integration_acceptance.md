# P3.3-S7-5 integration and acceptance reconciliation

Status: **Integration and verification complete within the approved bounded
scope; candidate ready for one substantive independent review. Not independently
approved, committed, published, or formal Phase 3.3 completion.**

Baseline inspected on 2026-09-20: `main`, HEAD/local `main`/local `origin/main`
all `f49ee3420365b8481b4a12dac6ea82d0b6f52d83`, ahead/behind 0/0, clean index
and worktree, 338 tracked files, no active Git operation. No fetch was performed.
S7-4 and its corrected 57-path implementation are approved and published at
that baseline. Earlier CHANGES_REQUIRED and frozen PROPOSED wording are history.

## Authority, scope and disposition

The [parent plan](phase_3_3_run_protocol_implementation_plan.md#requirement-to-slice-coverage-matrix),
[S7 allocation](phase_3_3_s7_1_post_ending_run_exit_plan.md#2-remaining-s7-allocation-in-dependency-order),
[S7-2 section 12](phase_3_3_s7_2_same_line_world_continuation_plan.md#12-later-allocation-findings-and-limits),
[S7-3 section 12](phase_3_3_s7_3_world_revisit_regional_progression_plan.md#12-remaining-ownership-documentation-and-review-handoff),
and [S7-4](phase_3_3_s7_4_canon_preserving_line_transition_run_completion_plan.md)
assign final reconciliation to S7-5. This candidate adds evidence and synchronizes
status under the user's S7-5 authorization; it introduces no new gameplay,
mutation, schema, content, identity or product decision requiring a new subdivision
design. The parent's pre-implementation subdivision rule is not a second planning
gate for exercising these already published contracts. A genuinely new transition
or authority would still require that rule; none is proposed here.

Criteria 10–11 retain their general invariants for **every supported authored
edge**, not merely the browser's chosen route. The approved S7-2 P06/section 5
contract explicitly freezes one required successor, priority 0, weight 1, no
random draw, visited-world exclusion, closed empty/singleton pools and rejection
of multiple candidates. S7-3 P03/P08 and section 5 explicitly authorize the held
receipt's important-world exception, one archive region, cooldown 0 completed
visits, one consumed entry and no reward. These are the implemented selection
rules, not missing generic algorithms renamed as future work. All three permitted
first-world ending IDs, both ending classes, all profiles, ineligible dispatch
endings, and consumed/terminal families are covered by applicable automated tests.
Browser evidence covers representative RESOLVED and FAILED routes, not all of them.

Required recovery is never defeated by weight or anti-repeat: the sole eligible
required edge wins; the held-receipt edge bypasses visited-world exclusion only
after its exact prerequisite. Explicit cancellation consumes nothing. The approved
S7-3 P02 amendment assigns destination/eligibility to the engine and consent to
the player. Missing required content is corruption, not a valid empty pool.
An ineligible ending or a consumed terminal route has no authored unfulfilled edge.

Important-NPC priority is **explicitly outside the currently executable phase
surface**: no separately published logical-identity/world predicate exists. The
parent expressly requires deterministic non-NPC progression to continue in its
absence. New tests enumerate all prohibited identity sources/relations and reject
both selector-level and pool-edge injection. They do not create such predicates.
General catalogue expansion, additional worlds, arbitrary revisits, branching or
replacement lines, NPC residence, generalized combat, memory platforms and
production Provider distribution remain outside these approved contracts.

No mandatory implementation blocker was found. One missing explicit parent
rejection matrix was supplied, alongside a stronger old/new journey read-isolation
check. No production defect, persistence change or migration was needed.

## Evidence key and reuse qualifications

External package root for this candidate:
`C:/Users/dylanmonster/.codex/visualizations/2026/09/20/01a0bd35-07da-75e0-8879-b7581e92fb20/s7-5-evidence`.
Its `evidence-reuse.json` records actual command selections/exits/log identities,
all historical source drift, dependency/environment comparisons and restoration
evidence. `candidate-manifest.json` and `candidate.patch` bind final bytes;
`review-handoff.md` supplies the single review protocol. Hashes remain external.

| Key | Exact evidence and applicability |
| --- | --- |
| F01 | Fresh `offline-focused.json/.log`: canonical `scripts/verify.ps1 -Mode Offline`, explicit eight-file selection, **214 passed**, one existing schema-shadow warning, exit 0; 46 new cases plus 168 existing selector/registry/mechanics/compiler/binding/projection cases. Compileall, pip check, single Alembic head/history `20260920_0011` and diff check also pass. This is focused verification despite the script's generic "full offline pytest" stage label. |
| R01 | S2 original `s3-implementation-20260916-audit/correction/s2-exhaustive.json/.log`, `s2-proof.json`, `s2-instrumentation.json`, `s2_counter.py`: exit 0, 23 passed, 104,165 maps, 2,812,455 distinct inputs, primary/repeat 2,812,455 each, **5,624,910 real calls**. Six relevant source/config identities and Python/platform/dependencies rechecked. Only the exhaustive node is reused; unavailable Turkish locale is retained. |
| R02 | S7-4 correction `offline-affected`: 178 passed, exit 0. Completion, admission replay, shared reconstruction, old continuation/revisit consumers on final Python implementation. Later drift is Web, documentation and SQL test additions, not these executed Python dependencies. |
| R03 | Correction `mysql-completion-final`: 71 passed, 721 deselected, exit 0; real MySQL public journeys, current-read exclusion, immutable replay, rollback/cancellation/CAS, observed races, 011 upgrade/downgrade/corruption. `mysql-final-preflight`: four additional final-inspection fault cases, exit 0; same migration bytes. The selections overlap; never sum them. |
| R04 | Correction `web-complete-candidate`: 433 passed, one existing optional presentation-probe skip, 14 suites, exit 0. `web-type-target-final`, `web-lint-target-final`, `web-build-final`: exit 0, deterministic-demo build. Final Web production/test identities match (build predates only a test addition). Rendered storage/race evidence is distinct from browser observation. |
| R05 | Original S7-4 `offline-final` is **failed**, exit 1: 2,916 passed, 772 skipped, one exhaustive-S2 deselection, 212 setup errors. Exact affected-node replacement is **211 passed + one Windows symlink privilege skip**, with four successful replacement command records, not their overlapping total. Reuse only unchanged S1/domain/legacy/content/script coverage; affected reconstruction/replay is superseded by R02/R03/R04. |
| R06 | Earlier S7-4 `mysql-canonical-02` 276 passed, `mysql-world-consumers-final` 117 passed, `mysql-final-shared` 44 passed, `mysql-migration-final` 44 passed, each exit 0. Historical supported-family/unchanged transaction proof only; not final corrected 011 or R1/R3 proof (R03 owns those). The earlier mixed-008/010-fixture failure and repair remain failures in retained records. Frozen008 internals use inspected `deviation-p33-s7-1-null-check-correction-20260918` commands/logs and exact migration/dependency identities, not current-fixture execution. |
| R07 | Original S7-4 `cross-process-export-final`: two passed, seeds 1/9173, complete traces/store exports for both source classes. Reused for valid-family determinism only; later independent content pin and corrupt-source checks do not change these approved inputs. No claim that old exports execute corrected failure paths. |
| B01 | S7-4 `s7-4-browser/report.md`, `evidence-index.md`, `acceptance.json`, `verification.json`, request windows and restart records at the same published commit. Both-source completion, cancellation, reload/history, fresh same-character entry/action, defer exit, eligible-seal voluntary exit, restart/clear/full reload. 40 screenshots; 527 backend response-status records, including 48 independent read-only observer GETs. |

Fresh comparison verified 505 artifacts in the correction/browser manifests and
all 57 published S7-4 candidate files byte-for-byte. Python 3.12.14, installed
Python package versions, Node 24.15.0, PowerShell 7.6.6 and dependency manifests
match the reused environment. Source drift at each older invocation is retained
and qualified above, not erased by publication. Applicable raw logs and manifests
are copied into the external package; originals and failed records remain intact.

## Complete parent acceptance matrix

Source paths below are relative to `src/deviation_protocol/`; test names are exact
searchable symbols. **Satisfied** means implementation/evidence within the
published contract, subject to the one pending independent candidate review.

| # / requirement | Actual implementation and applicable proof | Limits / disposition |
| --- | --- | --- |
| 1 Versioned profile/frozen protocol | `domain/run_protocol.py`, `run_protocol_binding.py`, `application/native_run_admission.py`; S1 `test_golden_vector_bytes_length_hash_and_round_trip_are_exact`, `test_real_maximum_legal_v1_envelope_has_independent_exact_identity` (R05); native codec/goldens/atomic binding (R02/R03/R06). | Legacy rows receive no synthesized profile; future versions fail closed. **Satisfied.** |
| 2 Defaults/overrides before first turn | `domain/run_protocol_resolution.py`, native admission resolve-before-staging; R01 exact exhaustive oracle/counters, R02 `test_native_preflight_has_no_uow_issuer_or_clock`, F01 public lattice/discovery. | Exhaustive evidence reused, not rerun or inferred from browser. **Satisfied.** |
| 3 Objective effects independent of presentation | Five independent policies in `domain/run_protocol_mechanics.py`, native turn coordinator; F01 `test_all_lattice_points_against_independent_arithmetic`, additive/saturation and prompt-independent presentation cross-product. | Frozen lattice/formulas only; no general combat. **Satisfied.** |
| 4 Separate character authority | Frozen admitted reference; `native_turn_mechanics.py` trusted binding; F01 `test_cross_binding_rejected` and identical state/events across presentations; R02 native binding/replay; R03 same-character later admission. | Fresh admission uses existing eligibility; no new character system. **Satisfied.** |
| 5 Atmosphere cannot mutate relationships | `run_protocol_prompt_context.py` presentation-only closed projection; F01 all off/veiled/charged combinations preserve objective snapshots/events/results. New forbidden-input vectors reject relationship/memory authority. | No Phase 3.4 relation/residence port is introduced. **Satisfied.** |
| 6 Deterministic outcomes | Pure resolution/policies/compiler, canonical selection seeds and immutable receipts; R01; F01 literal bytes, mapping order, repeat/process/hash-seed/available locale/no-I/O tests; R07 complete valid journey traces. | Only available locales, approved content and deterministic Demo, not external model output. **Satisfied.** |
| 7 Trusted frozen prompt inputs, no model authority | Authenticated `TrustedNativeTurnInputs`, stale/cross-bound rejection, pure compiler; F01 `test_forged_inputs_fail_before_fields`, `test_stale_and_mutated_context_rejected`, no-I/O and closed disclosure. R06 `tests/integration/test_mysql_native_turn_mechanics.py::test_adversarial_model_cannot_mutate_gameplay` and `test_compile_prompt_and_provider_have_no_sessions_or_mysql_locks` prove model rejection and transaction boundaries. | Completion is a fixed zero-Provider notice; no Live claim. **Satisfied.** |
| 8 Grim/Heroic/Chaotic limits | F01 `test_all_presentations_have_identical_objective_state_events_and_results` runs all 27 presentation combinations for three objective sets, compiler on/off; exact state/events/decision equality. R05 `tests/unit/test_story_director.py::test_fixed_deferred_mutable_and_dynamic_boundaries`; R06 adversarial model checks named above. | Expression never grants resource/success/canon authority. **Satisfied.** |
| 9 Fixed scenario facts | Independently pinned registry and `StoryDirector` trusted events; F01 pack substitution/byte/association checks; R02 `test_a01_a04_public_completion_and_immutable_history`, R03 unchanged old tables/snapshots; B01 visible hold/unproven/sealed truth. | Browser proves visible facts, not private full state. **Satisfied.** |
| 10 Entry freeze and deterministic progression/recovery | Native immutable entry binding; `world_continuation.py`, `session_content_registry.py`, continuation service; F01 closed edge/pool/seed/registry ordering and every unowned recovery input; R05 `test_other_resolved_source_ending_is_publicly_playable_and_eligible`; R02/R03 all profiles and classes; B01 both representative sources. | Approved empty/singleton pool has no nontrivial random weighting/tie. Required edge cannot lose. No NPC predicate means unavailable NPC priority, not blocked progression. **Satisfied; general expansion outside approved scope.** |
| 11 Meaningful state-preserving important-world revisit | `domain/world_revisit.py`, `application/session_service.py`, revisit service/persistence: sealed held base, distinct region/visit, preserved old roots/local NPCs/consequences, full PlayerState carry, one-use receipt. R02 `test_r03_actual_unlock_and_one_use_no_farming`, R03/R06 replay/races; F01 both completed/terminated old-history isolation; B01 depleted 3/6 and full visible history. | No reward economy; supported skill/cooldown maps empty. Synthetic inventory/wallet/zero fixtures are labelled, not acquired-gameplay claims. No authored recovery remains stranded. **Satisfied.** |
| 12 Documentation/status | This record, PLANS, Run Protocol, architecture, public contract and deferred register synchronized; complete tracked/new-file diff, links, UTF-8/LF/whitespace, protected identities and Git checks in `documentation-validation.json`. | Frozen plans/reviews preserved; formal phase closure still requires review/authorized commit/user publication/aligned baseline. **Satisfied for candidate; formal closure pending.** |

## S7-5 integration obligations and consumer checks

| Obligation | Connected implementation and evidence | Disposition |
| --- | --- | --- |
| Full playable chain and terminal alternatives | Native setup/admission → real first-world actions/ending → continuation → hold → archive/ending → explicit complete or exit → history → separately confirmed same-character admission/first action. R02/R03 public transport, R04 rendered actual Demo and B01 real page. F01 adds all old reads after new first action for both qualified completion and voluntary termination. | **Satisfied.** No direct state-injected browser route. |
| Legacy and all supported native families | Strict classifier separates standalone/legacy, internal native binding, original active3/terminated4, continued active4/terminated5, regional active5/terminated6, completed6. R02/R03/R05/R06, `test_a02_legacy_standalone_missing_and_strict_get_are_read_only`, closed family/domain cases. Dynamic Demo remains legacy-only. | **Satisfied.** Equal version numbers never imply equal lifecycle families. |
| Shared reconstruction and replay | SQL and Demo share prefix validation; `native_run_admission`, `native_turn_mechanics`, exit/continuation/revisit/completion, world-visit context and character eligibility consume exact families. Historical admission/actions/transitions retain immutable results after completion/new admission/retirement (R02/R03); fresh actions accept active current families only. | **Satisfied.** R1 ended-source live-job exclusion uses validated runtime ending/current locking reads; active archive live-job control retained. |
| Public projection and immutable associations | Explicit v1 old-family and v2 completed status/Journey; allowlisted separate completion projection, unchanged View/context/storage. Web `runCompletion.ts`, `runJourney.ts`, App compare retained confirmed POST identities against View/Journey/status/completion before enabling writes or adopting history. R04 A11, F01 projection tests, R02 API allowlists. | **Satisfied.** No private line/hash/receipt disclosure inferred from page. |
| Recovery, stale responses, history and no write replay | R04 exact attempt/URL/body/key retention; mismatched GET blocks uncertain retry, both read orders/client replacement/late responses; history storage unchanged, failed removal blocks setup. B01 cancel zero requests, each full history traversal 15 GET/0 POST, historical refresh returns third visit/completed, backend restart 8 GET/0 POST. | **Satisfied within observed scope.** Browser storage calls/order and per-click restart boundaries remain unverified; rendered instrumentation owns storage assertions. |
| Canon transition and no reward/reset | Same permanent line, `open_unresolved`→`closed_unresolved` only with approved seal provenance; completion changes only Run current/revision/receipt, historicalizes its binding; position and old state remain. R02/R03 exact map/table comparisons; B01 no visible refill/reward. Exit remains terminated with null completion. | **Satisfied.** No world-line replacement, resurrection or delivery proof. |
| Persistent atomicity, concurrency and migrations | R03 actual MySQL plus qualified R06/008 history cover required writers, failure prefixes, null checks, stale reads, lock races and restoration. New S7-5 paths add no mutation, SQL or concurrency behavior. | **Satisfied by applicable reuse.** No new database access; historical actual initial revision was 007, not 011, and initial/final full snapshots match. No claim about today's database state. |
| Deferred findings checkpoint | DF-001 process-local locale containment remains; F01 uses it, without claiming a two-locale fix. DF-002 original coupled startup race remains open; R04 return-to-setup GET controls and B01 ready/explicit-clear/full-reload containment still work. | **Eligible existing deferrals.** Owners/reassessment retained in canonical register; no additional release restriction. |

## Remaining limits, inventory and final review

B01 cannot establish SQL atomicity, full private PlayerState, private world-line
identity, storage-call ordering, request bodies or exact per-click restart counts.
The original restart checkpoint counter retained the prior epoch; the complete
new-backend log establishes only the overall eight GET/zero POST window. This
limitation is preserved. Browser Standard-profile play and rereading worlds 1/2
after new admission were not executed; automated all-profile evidence and F01
old-three-visit read checks supply their separate scopes. No new mandatory browser
scenario beyond the accepted authorization was identified or executed.

The broad historical Offline command remains failed, the symlink assertion remains
unexercised, and the optional Web probe remains skipped. Alembic online current/check
has no TEST_DATABASE_URL-only entrypoint and remains unrun; recorded real-MySQL
schema/enforcement/restoration tests provide that distinct proof. No .env read,
database connection, real Provider/Live call, dependency install, service launch,
subagent, staging, commit, push, tag, deployment or Git configuration change occurred.

Dependency-derived candidate inventory (seven paths):

- `tests/unit/test_s7_final_integration.py`: new explicit parent rejection vectors
  and public old/new journey isolation, 46 cases; no production changes.
- `docs/phase_3_3_s7_5_integration_acceptance.md`: this new acceptance owner.
- `PLANS.md`: publication/current candidate/readiness status.
- `docs/run_protocol.md`: publication and inspected S7-4 browser evidence pointer.
- `docs/architecture.md`: shipped completion and S7-5 integration boundary.
- `docs/public_client_contract.md`: published contract/recovery evidence status.
- `docs/engineering/deferred_findings.md`: existing checkpoint reassessment only.

Frozen plans, prior review/evidence text, all production/content/migration files,
existing tests, workflow/guardrails, dependencies and Git references/index remain
unchanged. External inventory/hashes cover every changed/new path and full patch.

Implementation is complete within the approved bounded contracts. Verification
is complete within the scopes/limits above. Candidate readiness is not independent
approval or production readiness. The parent still requires independent review,
separate exact commit authorization, manual user publication and clean aligned
baseline confirmation before formal Phase 3.3 completion. No post-commit independent
review stage is added.

Exactly one operative success token is specified **for the independent reviewer;
not issued by this authoring record**:
`PHASE_3_3_S7_5_FINAL_INTEGRATION_ACCEPTANCE_INDEPENDENT_REVIEW_APPROVED`.
Both APPROVED and APPROVED_WITH_DEFERRED_FINDINGS emit that same token and bind
all seven paths, full patch, baseline and external hashes. CHANGES_REQUIRED emits
no success token. Any changed candidate byte invalidates the binding. Historical
S1–S7-4 tokens are non-operative for this candidate. Review substance includes
all parent rows, scope interpretation, source/test/consumer applicability,
failure accounting, deferrals and synchronization, not merely document formatting.

## Guardrail impact

**None.** The new evidence closes a test-coverage gap under existing AUTH-001,
API-001 and parent rejection requirements; no newly reproduced production defect
or reusable rule change was identified. DF-001/DF-002 remain open and contained.
